{
  "name": "quibble-backend",
  "version": "1.0.0",
  "description": "Backend for Quibble premium request system",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "echo 'No build step required for Node.js'",
    "prod": "NODE_ENV=production node server.js",
    "functions:dev": "netlify dev",
    "functions:build": "netlify build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csrf": "^3.1.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "node-mailjet": "^6.0.9",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
